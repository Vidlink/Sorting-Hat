# Copy this file to .env and fill in your Supabase project values.
# Expo SDK 49+ requires the EXPO_PUBLIC_ prefix to expose variables to the JS bundle.

EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI key is NOT stored here. It's a Supabase Edge Function secret — set it via:
#   supabase secrets set OPENAI_API_KEY=sk-proj-...
# The app calls Whisper/GPT-4o through the transcribe-audio / openai-chat edge
# functions so the key never ships inside the app bundle.
